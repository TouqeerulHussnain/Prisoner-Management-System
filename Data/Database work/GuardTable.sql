create table GuardTable (	guardID int,
							guardName varchar(30),
							guardAddress varchar(50),
							guardPhone bigint,
							guardTime int,
							guardShift varchar(20)
							
							);

select * from GuardTable;